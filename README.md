# Atlas update feed

This repository hosts the public **update feed** for the Atlas macOS app.

`version.json` advertises the latest available version. The app checks it to notify users when a
newer release exists. The app's source lives elsewhere; only this feed is public.

Served via GitHub Pages at: https://bxdornx.github.io/atlas-updates/version.json
